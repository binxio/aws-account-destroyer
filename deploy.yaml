version: 0.2
phases:
  install:
    runtime-versions:
      docker: 18
      python: 3.7
  build:
    commands:
      ->  
        aws --profile mark-xebia cloudformation deploy 
        --stack-name aws-account-destroyer 
        --template-file ./aws-account-destroyer.yaml  
        --capabilities CAPABILITY_NAMED_IAM


